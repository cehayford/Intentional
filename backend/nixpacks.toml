# Nixpacks configuration for Backend
[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.build]
cmds = ["npm run build"]

[phases.install]
cmds = ["npm ci"]

[start]
cmd = "npm run start:prod"

[variables]
NODE_ENV = "production"
PORT = "3001"
